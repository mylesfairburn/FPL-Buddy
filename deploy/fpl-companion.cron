# /etc/cron.d/fpl-companion
#
# Two jobs on different clocks - see deploy/README.md for the reasoning.
#   deadline-watch : hourly, must be timing-accurate (FPL deadlines are irregular)
#   daily-refresh  : 03:00, deliberately late (BPS/data_checked lags late kickoffs)
#
# CHECK THE CONTAINER NAME FIRST. `docker exec` against a name that doesn't
# exist fails silently from cron - the job simply never runs, and the only
# symptom is that processed_deadline and ai_team_snapshot stay empty forever,
# which looks exactly like "the feature isn't working". Compose names containers
# <project>-<service>-<n>, so a stack in /root/apps gives apps-fpl-companion-1,
# NOT fpl-companion. Confirm with:
#
#   docker ps --format '{{.Names}}'
#
# Then edit CONTAINER below and:
#   cp deploy/fpl-companion.cron /etc/cron.d/fpl-companion && chmod 644 /etc/cron.d/fpl-companion
#
# No token is passed here on purpose. `docker exec` inherits the container's
# own environment, and FPL_REFRESH_TOKEN is already set there by compose, so
# reading it from a file on the host would just be a second copy to keep in
# sync. Both jobs call /api/refresh inside the container over 127.0.0.1.

SHELL=/bin/sh
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# TZ matters for the 03:00 slot only; the deadline watcher reads UTC timestamps
# from the API and doesn't care what the host clock is set to.
CRON_TZ=Europe/London

# The container name is written out in full on both lines rather than held in a
# variable. Variable expansion in cron.d depends on the job shell rather than on
# cron itself, and the failure mode if it doesn't expand is the silent one this
# file's header warns about. Two literals that are obviously wrong beat one
# indirection that might be.

# Hourly, at 5 past - a few minutes clear of the top of the hour so it isn't
# competing with every other cron on the box.
5 * * * * root docker exec apps-fpl-companion-1 python jobs.py deadline-watch >> /var/log/fpl-companion-watch.log 2>&1

# Daily heavy refresh + backfill of actual scores onto frozen snapshots.
0 3 * * * root docker exec apps-fpl-companion-1 python jobs.py daily-refresh >> /var/log/fpl-companion-daily.log 2>&1
